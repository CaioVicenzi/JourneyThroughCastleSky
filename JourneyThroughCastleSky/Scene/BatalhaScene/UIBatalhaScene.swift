//
//  UIBatalhaScene.swift
//  JourneyThroughCastleSky
//
//  Created by Caio Marques on 13/09/24.
//

import Foundation
import SpriteKit

/*
extension BatalhaScene {
    func setupNodes () {
        setupTitleEnemy()
        setupEnemyLife()
        setupMyLife()
        setupButtonAttack()
        setupButtonSpare()
        setupButtonUseItem()
        setupEnemy()
    }
    
    
    
    private func setupEnemyLife () {
        lifeEnemy.position = PositionHelper.singleton.rightUpCorner(lifeEnemy)
        lifeEnemy.position.y = lifeEnemy.position.y - 30
        lifeEnemy.fontSize = 20

        addChild(lifeEnemy)
    }
    
    private func setupMyLife () {
        myLife.position = PositionHelper.singleton.centralizeQuarterLeft(myLife)
        addChild(myLife)
    }
    
    
    
    private func setupEnemy () {
        enemySprite.position = PositionHelper.singleton.centralizeQuarterUp(enemySprite)
        enemySprite.setScale(0.4)
        enemySprite.position.y += 150
        enemySprite.position.x += 100
        addChild(enemySprite)
    }
}
*/
